# Ignore node_modules folder (for Firebase app)
firebase-app/node_modules/

# Ignore build outputs or temporary files (for ESP32)
embedded-code/*.bin
embedded-code/*.elf
embedded-code/*.hex

# Ignore local OS-specific files (e.g., macOS, Windows)
.DS_Store
Thumbs.db

# Ignore logs and temporary files
*.log
*.tmp

# Ignore sensitive config (Firebase service accounts, API keys)
firebase-app/.env

# Ignore IDE-specific settings (VSCode, IntelliJ, etc.)
.vscode/
.idea/
